# serve MSW mocks instead of the core-service API
VITE_API_MOCK=1

# bot hosting the mini app, without the @ (used for t.me invite links)
VITE_BOT_USERNAME=buddygym_bot
# mini app short name, only if the bot serves several apps
VITE_MINIAPP_NAME=app
